#ifndef USING_h

#define USING_h

namespace IdeaNet{
    const int PDF_SERVER_MAX_REQ_SIZE = 1024*1024;
    const int PDF_SERVER_PORT = 3344;
    const std::string NONE = "";

//    #ifdef WIN32
//	const QString TEMP_PDF_PNG_PATH = (PDF_SERVER_DIR + "\\" + TEMP_PDF_PNG);
//    #else
//	const QString TEMP_PDF_PNG_PATH = (PDF_SERVER_DIR + "/" + TEMP_PDF_PNG);
//    #endif
}

#endif /* end of include guard: USING_h */
